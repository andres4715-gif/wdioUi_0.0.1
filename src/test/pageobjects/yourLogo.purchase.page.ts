import OpenPagePage from './openPage.page';

/**
 * sub page containing specific selectors and methods for a specific page
 */
class FillForm extends OpenPagePage {

    openYourLogoPage() {
        return super.openYourLogoPage();
    }
}

export default new FillForm();
